{
  "name": "NutriBot — Error Workflow",
  "nodes": [
    {
      "id": "n8n-j6dv2j7k",
      "name": "Error Trigger",
      "type": "n8n-nodes-base.errorTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "parameters": {}
    },
    {
      "id": "n8n-cuje4ljt",
      "name": "Redact & Log Error",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        260,
        0
      ],
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Redação básica antes de logar/alertar — nunca deixa token/e-mail em claro\n// (mesma regra de src/redact.js).\nconst SECRET_KEY_PATTERN = /token|secret|password|senha|authorization|api[_-]?key/i;\nconst EMAIL_PATTERN = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n\nfunction maskEmail(v) {\n  if (typeof v !== 'string' || !EMAIL_PATTERN.test(v)) return v;\n  const [user, domain] = v.split('@');\n  return user.slice(0, 2) + '*'.repeat(Math.max(user.length - 2, 1)) + '@' + domain;\n}\n\nfunction redact(value, depth) {\n  depth = depth || 0;\n  if (depth > 6 || value == null) return value;\n  if (Array.isArray(value)) return value.map((v) => redact(v, depth + 1));\n  if (typeof value === 'object') {\n    const out = {};\n    for (const k of Object.keys(value)) {\n      if (SECRET_KEY_PATTERN.test(k)) out[k] = '***redacted***';\n      else if (typeof value[k] === 'string' && EMAIL_PATTERN.test(value[k])) out[k] = maskEmail(value[k]);\n      else out[k] = redact(value[k], depth + 1);\n    }\n    return out;\n  }\n  if (typeof value === 'string' && EMAIL_PATTERN.test(value)) return maskEmail(value);\n  return value;\n}\n\nconst err = $json;\nconst entry = {\n  timestamp: new Date().toISOString(),\n  level: 'error',\n  event: 'workflow.failed',\n  workflow: err.workflow ? err.workflow.name : null,\n  node: err.execution && err.execution.lastNodeExecuted,\n  message: err.execution && err.execution.error ? err.execution.error.message : String(err),\n};\nconsole.error(JSON.stringify(redact(entry)));\nreturn { json: entry };"
      }
    },
    {
      "id": "n8n-7cl7jdfh",
      "name": "Alert (configure Slack/Email aqui)",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [
        520,
        0
      ],
      "notes": "Placeholder deliberado: plugue aqui o node de Slack/Email/PagerDuty da sua organização. Mantido como NoOp para não assumir credenciais que não existem neste repo. Ver docs/production-checklist.md — alerta é obrigatório antes de ativar produção.",
      "parameters": {}
    }
  ],
  "connections": {
    "Error Trigger": {
      "main": [
        [
          {
            "node": "Redact & Log Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Redact & Log Error": {
      "main": [
        [
          {
            "node": "Alert (configure Slack/Email aqui)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Alert (configure Slack/Email aqui)": {
      "main": []
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {},
  "versionId": "n8n-og56helc"
}
