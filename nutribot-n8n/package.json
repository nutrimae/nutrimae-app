{
  "name": "nutribot-n8n",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Orquestração do NutriBot (n8n + Z-API + Typebot + PostgreSQL) — lógica de roteamento, dedup e sessão, testada independente do runtime do n8n.",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "generate:workflow": "node tools/generate-workflow.mjs",
    "smoke:workflow": "node tools/smoke-test-workflow-code.mjs",
    "verify": "npm run generate:workflow && npm run smoke:workflow && npm test"
  },
  "engines": {
    "node": ">=18.17"
  }
}
